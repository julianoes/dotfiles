# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout somewhen, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod4

font pango:Inconsolata Medium 9


# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal
bindsym $mod+Return exec i3-sensible-terminal

# kill focused window
bindsym $mod+Shift+q kill

# start dmenu (a program launcher)
#bindsym $mod+d exec dmenu_run
bindsym $mod+d exec rofi -show drun
# There also is the (new) i3-dmenu-desktop which only displays applications
# shipping a .desktop file. It is a wrapper around dmenu, so you need that
# installed.
# bindsym $mod+d exec --no-startup-id i3-dmenu-desktop

# change focus
# changed from the default to match vi
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+h move left
bindsym $mod+Shift+j move down
bindsym $mod+Shift+k move up
bindsym $mod+Shift+l move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+v split h

# split in vertical orientation
bindsym $mod+s split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+q layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

# focus the child container
bindsym $mod+Shift+a focus child

# switch to workspace
bindsym $mod+1 workspace 1:comm
bindsym $mod+2 workspace 2:todo
bindsym $mod+3 workspace 3:web
bindsym $mod+4 workspace 4:code4
bindsym $mod+5 workspace 5:code5
bindsym $mod+6 workspace 6:qgc
bindsym $mod+7 workspace 7:debug
bindsym $mod+8 workspace 8:etc8
bindsym $mod+9 workspace 9:etc9
bindsym $mod+0 workspace 10:music

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace 1:comm
bindsym $mod+Shift+2 move container to workspace 2:todo
bindsym $mod+Shift+3 move container to workspace 3:web
bindsym $mod+Shift+4 move container to workspace 4:code4
bindsym $mod+Shift+5 move container to workspace 5:code5
bindsym $mod+Shift+6 move container to workspace 6:qgc
bindsym $mod+Shift+7 move container to workspace 7:debug
bindsym $mod+Shift+8 move container to workspace 8:etc8
bindsym $mod+Shift+9 move container to workspace 9:etc9
bindsym $mod+Shift+0 move container to workspace 10:music

# Use mod-Control-Up and Down to rotate through the workspace list.
bindsym $mod+Control+Up workspace prev
bindsym $mod+Control+Down workspace next

# Use mod-Shift-Control-Up and Down to move the selected window to an
# adjacent workspace. (This does *not* change your view to that workspace,
# it just "zaps" the window there.)
bindsym $mod+Shift+Control+Up move workspace prev
bindsym $mod+Shift+Control+Down move workspace next

# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -b 'Yes, exit i3' 'i3-msg exit'"

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode
        set $resizestep 5
        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym h resize shrink width $resizestep px or $resizestep ppt
        bindsym j resize grow height $resizestep px or $resizestep ppt
        bindsym k resize shrink height $resizestep px or $resizestep ppt
        bindsym l resize grow width $resizestep px or $resizestep ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width $resizestep px or $resizestep ppt
        bindsym Down resize grow height $resizestep px or $resizestep ppt
        bindsym Up resize shrink height $resizestep px or $resizestep ppt
        bindsym Right resize grow width $resizestep px or $resizestep ppt

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

bindsym $mod+r mode "resize"

set_from_resource $darkred     color1  #FF0000
set_from_resource $red         color9  #000000
set_from_resource $darkgreen   color2  #000000
set_from_resource $green       color10 #000000
set_from_resource $darkyellow  color3  #000000
set_from_resource $yellow      color11 #000000
set_from_resource $darkblue    color4  #000000
set_from_resource $blue        color12 #000000
set_from_resource $darkmagenta color5  #000000
set_from_resource $magenta     color13 #000000
set_from_resource $darkcyan    color6  #000000
set_from_resource $cyan        color14 #000000
set_from_resource $darkwhite   color7  #cccccc
set_from_resource $white       color15 #ffffff
# Use custom colors for black
set $black       #2e3440CC
set $darkblack   #3b4252CC
set $transparent #00000000

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
bar {
  status_command i3blocks
  #i3bar_command i3bar -t
  #status_command ~/.i3/i3status-pomodoro.sh
  #status_command i3status -c ~/.i3/i3status.conf
        # mode hide
  #tray_output primary
  tray_padding 0
  position bottom
  separator_symbol "|"
  #height 25
  font pango:Inconsolata Medium 14
  colors {
    statusline         $white
    background         $transparent
    separator          $white

#                      BORDER       BACKGROUND   TEXT
    focused_workspace  $white       $white       $darkblack
    inactive_workspace $transparent $transparent $white
    active_workspace   $transparent $transparent $white
    urgent_workspace   $darkred     $darkred     $transparent
    binding_mode       $darkred     $darkred     $transparent
  }

}

# hide edge borders
# hide_edge_borders <none|vertical|horizontal|both>
hide_edge_borders both
#for_window [class=".*"] border pixel 0
#:smart_borders on

# press same workaspace number again to switch back to last workspace
workspace_auto_back_and_forth yes

# configure default workspace for some applications
assign [class="Skype"] 1:comm
#assign [class="Chromium"] 2:web
assign [class="Qgroundcontrol"] 6:qgc
#assign [class="osgViewer"] 4:groundcontrol

#exec compton

# don't turn off monitor after a while
#exec xset dpms force off

# Set up monitors correctly
#exec ~/Scripts/monitor_layout.sh internal

#exec --no-startup-id nm-applet
#exec --no-startup-id dbus-launch nm-applet

# autostart
#exec insync start
#exec dropbox
#exec skype %U
#exec /usr/bin/chromium --no-startup-window
#exec ssh-agent
#exec ssh-add
exec redshift-gtk -l 47.376888:-8.541694

# moving workspaces
bindsym $mod+x move workspace to output right
bindsym $mod+p move container to output VGA1

# Make the currently focused window a scratchpad
bindsym $mod+Shift+minus move scratchpad

# Show the first scratchpad window
bindsym $mod+minus scratchpad show

# dynamic tagging feature
set $control ~/.i3/workspace_controller.py
bindsym $mod+t exec $control dynamic go
bindsym $mod+Shift+t exec $control dynamic move

# use Fn+F6 to Fn+F8 for volume control
#bind 121 exec pactl set-sink-mute alsa_output.pci-0000_00_1f.3.analog-stereo toggle
#bind 122 exec pactl set-sink-volume alsa_output.pci-0000_00_1f.3.analog-stereo -2%
#bind 123 exec pactl set-sink-volume alsa_output.pci-0000_00_1f.3.analog-stereo +2%
# and Shift+F6 to Shift+F8
#bind Shift+72 exec pactl set-sink-mute alsa_output.pci-0000_00_1f.3.analog-stereo toggle
#bind Shift+73 exec pactl set-sink-volume alsa_output.pci-0000_00_1f.3.analog-stereo -2%
#bind Shift+74 exec pactl set-sink-volume alsa_output.pci-0000_00_1f.3.analog-stereo +2%

# use Shift+F4 to use internal display only
bind Shift+70 exec ~/Scripts/monitor_layout.sh internal

# use Shift+F5 to use external monitor only
bind Shift+71 exec ~/Scripts/monitor_layout.sh external

# use Fn F2 and F3 for brightness
#bind 232 exec xbacklight -dec 5
#bind 233 exec xbacklight -inc 5
# and Shift+F2 and Shift+F3 as well
#bind Shift+68 exec xbacklight -dec 5
#bind Shift+69 exec xbacklight -inc 5

# keybindings for some applications
bindsym $mod+Control+c exec firefox
bindsym $mod+Control+t exec gnome-terminal
bindsym $mod+Control+g exec qgroundcontrol
bindsym $mod+Control+n exec nautilus
bindsym $mod+Control+v exec gvim

# wallpaper
exec feh --bg-fill ~/Pictures/background.jpg

# pomodoro disable comm workspace
exec python ~/src/i3-gnome-pomodoro/pomodoro-client.py daemon 1 &

# monitor setup
exec ~/Scripts/monitor_layout.sh

# gnome logout
#bindsym $mod+Shift+i exec gnome-session-quit --logout

# mode_system for shutdown, reboot etc. from https://faq.i3wm.org/question/239/how-do-i-suspendlockscreen-and-logout/
set $mode_system System (l) lock, (e) logout, (s) suspend, (h) hibernate, (r) reboot, (Shift+s) shutdown
mode "$mode_system" {
    bindsym l exec --no-startup-id ~/.i3/i3exit lock, mode "default"
    bindsym e exec --no-startup-id ~/.i3/i3exit logout, mode "default"
    bindsym s exec --no-startup-id ~/.i3/i3exit suspend, mode "default"
    bindsym h exec --no-startup-id ~/.i3/i3exit hibernate, mode "default"
    bindsym r exec --no-startup-id ~/.i3/i3exit reboot, mode "default"
    bindsym Shift+s exec --no-startup-id ~/.i3/i3exit shutdown, mode "default"

    # back to normal: Enter or Escape
    bindsym Return mode "default"
    bindsym Escape mode "default"
}
bindsym $mod+Home mode "$mode_system"

#                       BORDER      BACKGROUND  TEXT        INDICATOR   CHILD_BORDER
client.focused          $black      $darkblack  $white      $white      $darkblack
client.unfocused        $black      $black      $darkwhite  $darkblack  $darkblack
client.focused_inactive $black      $black      $darkwhite  $darkblack  $darkblack
client.urgent           $darkred    $darkred    $black      $darkred    $darkred
client.background $black
